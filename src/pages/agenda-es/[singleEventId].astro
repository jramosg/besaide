---
import SingleEvent from '@/components-pages/SingleEvent.astro';
import { SingleEventStaticPaths } from '@/staticPathsHelper';
import type { GetStaticPathsOptions } from 'astro';

export function getStaticPaths({}: GetStaticPathsOptions) {
	return SingleEventStaticPaths('es');
}

const { event } = Astro.props;
---

<SingleEvent event={event} />
